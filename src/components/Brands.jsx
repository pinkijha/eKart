import React from 'react'
import Header from './Header'

const Brands = ({}) => {
  return (
    <div>
      <Header  currentPage={currentPage} togglePage={togglePage}  />
      <div>

      </div>

      <div>
        <table>
          <th>Name</th>
        </table>
      </div>
    </div>
  )
}

export default Brands
